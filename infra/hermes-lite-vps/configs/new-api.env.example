# Copy to /root/new-api/.env and chmod 600.
# Keep password length compatible with the deployed new-api build.

ROOT_EMAIL=admin@example.com
ROOT_PASSWORD=replace_me_8_to_20_chars
ROOT_TOKEN=replace_me
TZ=Asia/Shanghai
